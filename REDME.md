# 注塑件模具修模分析工具 —— 项目框架

## 一、项目目的

基于三坐标(CMM)测量得到的 X/Y 坐标数据(Z 轴不使用),拟合出理论圆,
计算实测轮廓相对理论圆的偏差,通过极坐标偏差图直观展示,并结合"型腔/型芯"
判断逻辑,给出修模方向建议,最终可导出 PDF 报告。

---

## 二、UI 要求

- 界面风格:**Fluent Design**(推荐 `PyQt-Fluent-Widgets`,基于 PySide6)
- 支持**深色模式**(跟随系统 / 手动切换)
- 适配**高 DPI 屏幕**(程序入口需设置 DPI 缩放策略)

---

## 三、核心业务逻辑

```
导入 X,Y 坐标点(无序,Z 轴若存在则忽略)
    ↓
最小二乘法拟合圆心 (x0, y0) 和半径 r0
    ↓
对每个点计算 θ = atan2(y - y0, x - x0),按角度排序(0°~360°)
    ↓
计算每个角度下的偏差 Δr(θ) = r(θ) - r0
    ↓
用户选择该特征对应模具的"型腔"或"型芯"
    ↓
输出:极坐标偏差图 + 按角度排序的修模建议表 + 圆度值
    ↓
导出 PDF 报告 / CSV 数据
```

### 修模方向判断规则表

| 特征类型 | Δr > 0(实测偏大) | Δr < 0(实测偏小) |
|---|---|---|
| 型腔(决定外形) | 该方向需**缩小**型腔 | 该方向需**放大**型腔 |
| 型芯(决定内孔) | 该方向需**放大**型芯 | 该方向需**缩小**型芯 |

### 圆度值

```
圆度值 = max(Δr) - min(Δr)
```

用一个数字概括整体轮廓质量,显示在结果面板顶部。

---

## 四、图表功能设计

### 1. 坐标系自由切换
- **平面坐标系视图**:X-Y 散点/轮廓线,可叠加拟合圆作为参考
- **极坐标视图**:主偏差分析视图
- 用 Segmented Toggle 切换,共用同一份数据,只切换渲染方式

### 2. 图层开关(CheckBox 控制,互不影响)

| 图层 | 说明 |
|---|---|
| 理论拟合圆 | 显示 r0 参考圆(虚线) |
| 实测轮廓 | 实际测得点连线 |
| 圆心标记 | 显示拟合出的 (x0, y0) |
| 偏差数值标注 | 标出最大正/负偏差点及角度 |
| 距圆心距离标注 | 每个点旁标注 r(θ) 数值 |

### 3. 偏差放大倍数(极坐标视图专属)

```
显示半径 = r0 + k × Δr(θ)
```

- `k` 用 Slider 控制(建议范围 1~500)
- 仅影响**显示**,不影响 `mold_advisor.py` 中用于计算建议的真实 Δr 数值
- 可给一个基于偏差量级的推荐初始值,让偏差在视觉上占轮廓的约 10%

### 4. 偏差方向颜色编码
- `Δr > 0` 与 `Δr < 0` 用不同颜色区分(如红/蓝),纯视觉优化

### 5. 图表与表格联动高亮
- 点击建议表格中的一行,图上对应点高亮;反之亦然

### 6. 单位切换
- 偏差数值可在 mm / μm 之间切换显示(不影响底层数据)

---

## 五、导出功能

### 1. PDF 报告(`report_generator.py`)

报告内容:
1. 基本信息:项目名/零件名(可选填)、测量日期、特征类型(型腔/型芯)
2. 拟合结果:圆心坐标、拟合半径、点数、圆度值
3. 极坐标偏差图(当前图表状态,含放大倍数,导出为图片嵌入)
4. 修模建议表:按角度排序,列出角度、实测偏差、修模方向建议

### 2. CSV 数据导出
- 一键导出修模建议表为 CSV,方便导入 Excel 二次处理

### 3. 会话保存/加载(单特征版)
- 将当前分析的原始点 + 设置(型腔/型芯选择、放大倍数等)保存为 JSON
- 下次可加载继续查看,无需重新导入数据

---

## 六、目录结构

```
项目根目录/
├── main.py                        # 入口,高DPI设置 + 主题初始化
├── ui/
│   ├── main_window.py             # 主窗口(FluentWindow)
│   ├── theme_manager.py           # 深色/浅色切换
│   └── widgets/
│       ├── data_import_widget.py  # 数据导入(粘贴/CSV/Excel)
│       ├── fit_result_widget.py   # 拟合圆心/半径/圆度值 + 型腔型芯选择
│       ├── chart_widget.py        # 平面/极坐标图,放大倍数、图层开关、联动高亮
│       └── advice_table_widget.py # 修模建议表格
├── core/
│   ├── data_loader.py             # 解析X,Y(自动过滤Z列)
│   ├── circle_fitting.py          # 最小二乘拟合(代数法/Kasa法)
│   ├── coordinate_transform.py    # 笛卡尔→极坐标 + 按角度排序
│   ├── deviation_analysis.py      # Δr(θ)计算,含圆度值、最大正/负偏差点
│   ├── mold_advisor.py            # 型腔/型芯规则表 → 修模方向建议
│   └── report_generator.py        # 生成 PDF 报告
├── models/
│   └── data_models.py
│       ├── MeasurePoint(x, y)
│       ├── FittedCircle(x0, y0, r0, residual)
│       ├── FeatureType(Enum: CAVITY / CORE)
│       └── PolarPoint(theta, r, delta_r, advice)
└── resources/
    └── icons/
```

---

## 七、技术选型

| 功能 | 库 |
|---|---|
| UI 框架 | PyQt-Fluent-Widgets(基于 PySide6) |
| 数值计算 | numpy |
| 圆拟合 | scipy.optimize.leastsq 或代数法(Kasa) |
| 极坐标绘图 | matplotlib(polar projection,嵌入 Qt) |
| 数据导入 | pandas(支持 CSV/Excel) |
| PDF 报告生成 | reportlab / 或图片+表格排版方案(实现时用专门技能处理) |
| 会话保存 | JSON |

---

## 八、预留扩展点(本次不实现,仅留接口)

| 位置 | 预留方式 |
|---|---|
| `data_loader.py` | 预留 `validate_points()` 空函数,以后做离群点检测/点数校验 |
| `coordinate_transform.py` | 预留 `theta_offset=0` 参数,以后做"角度零点基准"(对应模具/零件实际方向) |
| `models/data_models.py` | 用 dataclass 定义,以后加字段(如离群点标记)不破坏现有结构 |
| `mold_advisor.py` | 以规则表(dict/函数映射)形式实现,以后加公差带判断只改这一个函数 |
| 项目整体 | 当前为单特征(Feature)分析,以后升级为多特征/多零件项目管理(List[Feature])改动量小 |

以下功能明确**暂不实现**,仅作为未来方向记录:
- 离群点自动检测算法
- 公差带判断与超差报警
- 修模前后数据对比叠加显示
- 多特征/多零件项目管理
- 角度零点基准的精细化设置(与模具/零件实际基准对应)

---

## 九、开发顺序建议

1. `circle_fitting.py` + `coordinate_transform.py`(核心算法,先用假数据验证)
2. `chart_widget.py`(平面/极坐标绘图 + 放大倍数 + 图层开关,交互效果需要反复调整)
3. `mold_advisor.py` + `deviation_analysis.py`(修模建议逻辑 + 圆度值)
4. `report_generator.py`(PDF 报告导出)
5. UI 整体拼装(Fluent 主窗口 + 各 widget 组合 + 深色模式/高DPI)
